{
    "name": "rails-latest-migration",
    "displayName": "rails-latest-migration",
    "description": "Opens the latest migration created in a Ruby on Rails app.",
    "version": "0.0.1",
    "publisher": "tmikoss",
    "engines": {
        "vscode": "^1.12.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.railsLatestMigration"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.railsLatestMigration",
            "title": "Rails Latest Migration"
        }]
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    },
    "license": "MIT"
}
